MatchData (id, coefA,coefB,winSide)


PlayerData (id,action,[matchId],amount,[betSide])
	//action 0==deposit
	//	 1==bet
	//	 2==withdraw

Player (id,amount,playCt,winCt,illegalBool)


1. arraylist for matchdata ArrayList<MatchData>
2. arraylist for playerdata ArrayList <PlayerData>
3. arraylist for player ArrayList<Player>
4. check if playerdata is legitimate
5. for el in ArrayList<PlayerData>:
	if action=0,1,2
	


